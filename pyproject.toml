[build-system]
requires = [
    "setuptools>=42",
    # TODO : coordinate versions with PYPI; until then use conda
    # "matplotlib",
    # "opencv-python",
    # "torch==1.6.0",
    # "scipy",
    # "tensorboard",
    # "torchvision==0.7.0",
]
build-backend = "setuptools.build_meta"